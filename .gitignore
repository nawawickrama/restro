*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules

# Note: /public/build is deliberately NOT ignored. The restaurant runs on
# shared hosting with no Node, so the compiled CSS, JS and fonts have to ship
# with the repository. After changing anything in resources/, run
# `npm run build` and commit the result — see the README.
/public/build.zip

# Menu photos uploaded by staff. These belong to the running restaurant, not
# to the source: a clone should start with an empty gallery.
/storage/app/public/menu-items
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
